
# Git

To view `changes to be committed`, `changes not staged` and `untracked files`.

```bash
git status
```

To push commit from origin to main branch

```bash
git add <file>
git commit -m "<commit-message>"
git push -u origin main
```

To change the name of a file
```bash
git mv <old-name> <new-name>
```